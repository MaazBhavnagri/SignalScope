# SignalScope - Python dependencies (Python 3.10-3.12)
# PyTorch: install the build for your hardware first, e.g.
#   CUDA 12.x : pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU only  : pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchvision>=0.16
timm>=1.0.9
numpy>=1.24,<2.0
pillow>=10.0
scikit-learn>=1.3
scipy>=1.10
pyarrow>=14.0
huggingface_hub>=0.20
open_clip_torch>=2.24          # Bonus E (caption consistency); lazy-loaded, optional at runtime
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
sqlalchemy>=2.0
pydantic>=2.5
httpx>=0.27
pytest>=8.0
